# GitRank frontend environment.
#
# VITE_API_URL is the primary — this is the API base for the deployed backend.
# VITE_API_BASE is honoured for backwards compatibility.
VITE_API_URL=http://localhost:3000
