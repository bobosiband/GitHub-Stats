/** Cohort shell — header + underline tabs + <Outlet/>. Filled in Phase 4. */
import { Outlet } from 'react-router-dom';
export default function Cohort() {
  return (
    <div className="container">
      Cohort (placeholder) <Outlet />
    </div>
  );
}
